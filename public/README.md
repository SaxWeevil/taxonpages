# Public

This folder contains the public assets used by TaxonPages.

Files placed here are served directly from the project root and can be referenced using absolute paths.

For example, `public/favicon.svg` is available at `/favicon.svg`.
